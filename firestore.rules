rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /users/{userId} {
      allow read, write: if true; // In development, allow reading/writing user profiles
    }
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
